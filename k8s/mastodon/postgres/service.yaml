---
apiVersion: v1
kind: Service
metadata:
  name: mastodon-postgres
  namespace: mastodon
spec:
  ports:
    - port: 5432
      targetPort: 5432
  selector:
    app: mastodon-postgres
  type: ClusterIP
